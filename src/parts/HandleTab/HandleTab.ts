export const handleTab = (editor: any) => {
  // TODO
  // 1. if completion is open, insert completion item
  // 2. try to execute tab completion provider
  // 3. enter tab or two spaces

  console.log('tab')
  return editor
}
