import type { CodeGeneratorState } from '../CodeGeneratorState/CodeGeneratorState.ts'

export const codeGeneratorAccept = (state: CodeGeneratorState): CodeGeneratorState => {
  // TODO close code generator widget
  return state
}
