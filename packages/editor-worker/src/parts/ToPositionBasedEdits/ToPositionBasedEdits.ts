import type { OffsetBasedEdit } from '../OffsetBasedEdit/OffsetBasedEdit.ts'

export const toPositionBasedEdits = (editor: any, offsetBasedEdits: readonly OffsetBasedEdit[]): readonly any[] => {
  const newEdits: any[] = []
  return newEdits
}
